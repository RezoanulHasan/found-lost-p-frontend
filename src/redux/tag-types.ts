export enum tagTypes {
  users = "users",
  profile = "profile",
  lost = "lost-items",
  found = "found",
}

export const tagTypesList = [
  tagTypes.users,
  tagTypes.profile,
  tagTypes.lost,
  tagTypes.found,
];
